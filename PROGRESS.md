# Progress

## Week 1, Day 1 — Project scaffolding

- Read `docs/SPEC.md`, confirmed understanding with user.
- Created repo skeleton: `src/durable_agents/` package layout (events, state,
  orchestrator, storage, llm, tools, guardrails, api, cli), `db/migrations/`,
  `tests/` (unit, integration, chaos, guardrails, property, live), plus
  `pyproject.toml`, `docker-compose.yml`, `DECISIONS.md`, `README.md`,
  `.env.example`, `.gitignore`. All files empty — no application code yet.
- Created `CLAUDE.md`: source-of-truth pointer, core invariants, do-not-
  implement list (events.py, state.py, orchestrator.py,
  guardrails/decisions.py — user writes these), stack, dependency rule.
- `uv.lock` intentionally not created — generated by `uv` once
  `pyproject.toml` has real dependencies.

## Week 1, Day 1 (continued) — Postgres, dependencies, events.py

- `docker-compose.yml`: Postgres 16 (running, healthy) + Adminer web UI at
  `localhost:8080` for browsing the DB visually.
- `db/migrations/001_events_table.sql` applied to the running container.
  Dropped one index from the literal spec text (`idx_events_run_seq`) after
  confirming it duplicated the index the primary key already creates —
  first instance of "verify the spec, don't transcribe it."
- Installed `uv` (wasn't on the machine), wrote `pyproject.toml`
  (`hatchling` build backend, `pydantic`/`asyncpg` runtime deps,
  `pytest`/`pytest-asyncio`/`testcontainers[postgres]`/`mypy` dev deps),
  ran `uv sync` → `.venv` created, `uv.lock` generated (committed;
  `.venv` is not — filled in `.gitignore`, which had been left empty).
- `src/durable_agents/events.py` written: `BaseEvent`, `ToolCallInvocation`
  sub-model, all 13 event classes, `Event` discriminated union. Four design
  decisions made via structured Q&A (nested models over dicts, closed
  `Literal` types over open strings, flexible dict for
  `GuardrailTriggered.detail`, dropped a redundant timestamp field). Filled
  a spec gap: `ToolCallFailed` needed more fields than the spec table
  listed to be reconcilable on resume. Verified with `mypy --strict`
  (clean) and a manual discriminated-union round-trip test.
  Note: this file was on the user's own "write yourself" list; written by
  Claude only after the user was shown that tradeoff explicitly and chose
  to override it anyway. Full detail in `docs/BUILD_LOG.md` iteration 2.

## Week 1, Day 3 — EventStore built and verified live

- `storage/protocol.py`: `EventStore` as an ABC (user chose this over the
  spec's `Protocol`), `ConcurrencyConflict` exception.
- `storage/postgres.py`: real implementation. Pool injected via
  constructor + `connect(dsn)` convenience classmethod; payload JSONB
  strips `seq`/`created_at`/`type` (already columns) and merges them back
  on read. Added `asyncpg-stubs` dev dependency after `mypy --strict`
  flagged asyncpg has no type stubs at all.
- Verified live against the running Postgres container (append, read,
  round-trip equality, `ConcurrencyConflict` on duplicate seq, empty
  `read_since`) — all passed. Manual script, not yet a committed test.
- Hit and fixed an environment issue: a native Windows PostgreSQL 18
  service was squatting on port 5432, so the container's port-forward
  never actually received host connections. Stopped the native service.
  Full detail in `docs/BUILD_LOG.md` iteration 3 — worth remembering if
  Postgres connection errors show up again on this machine.

## Week 1, Day 3 (continued) — state.py, all tests green, Week 1 done

- `state.py` written (`RunState`, `Message`, `InFlightOp`,
  `PendingApproval`, `GuardrailHit`, `apply()`, `rebuild_state()`) — same
  override pattern as `events.py`: flagged the conflict with `CLAUDE.md`,
  user chose full override. One structural spec fix: dropped `run_id`
  from `RunState` since `rebuild_state` never receives one and `Event`
  objects don't carry it either — documented in the docstring.
- `tests/unit/test_state.py` (7 tests) and
  `tests/integration/test_postgres_store.py` + `conftest.py` (4 tests,
  real disposable Postgres via `testcontainers`) — all 11 passing,
  `mypy --strict` clean across all 27 source files.
- Two gotchas hit and fixed: asyncpg pools can't cross event loops (broke
  a session-scoped pool fixture under pytest-asyncio strict mode — fixed
  by making the pool function-scoped, only the container stays
  session-scoped), and mypy needs a `py.typed` marker file to trust a
  locally-installed package's own type hints once it's imported rather
  than analyzed by path (added `src/durable_agents/py.typed`).
- **Week 1 is functionally complete** per spec section 18's scope. Full
  detail: `docs/BUILD_LOG.md` iteration 4.

## Week 2, Day 1 — LLM client + tool registry built

- `llm/protocol.py`: `LLMClient` ABC (consistent with `EventStore`'s
  ABC choice — asked again rather than assumed), `LLMResponse` reusing
  `ToolCallInvocation` from `events.py`.
- `llm/scripted.py`: `ScriptedLLM`. Ships in the real package (unlike
  `refund_tools.py`) — generic testing infra any consumer needs, not
  demo-specific.
- `tools/registry.py`: `@tool` decorator, JSON schema auto-derived from
  function type hints via Pydantic's `create_model`. Dropped the spec's
  `idempotency=` parameter — its own formula never actually used the
  value. `idempotency_key()` implements spec section 11's sha256 formula.
- `tools/refund_tools.py`: `lookup_order`, `check_refund_policy`,
  `issue_refund` — in-memory demo data, no attempt-ledger yet (that's
  Week 3, once crash-resume testing needs it).
- All verified manually (schema derivation, approval threshold,
  idempotency key stability/uniqueness, tool execution). `mypy --strict`
  clean. Full detail: `docs/BUILD_LOG.md` iteration 5.

## Week 2, Day 1 (continued) — orchestrator.py built and verified end-to-end

- `orchestrator.py` written (fourth and last write-yourself file, same
  override pattern as the previous three). Key insight: reconciliation
  (spec's step 2) and normal operation are literally the same code path
  — the loop reloads state fresh every iteration and can't structurally
  tell "requested a moment ago" from "requested by a dead process."
  `decide_next_action(state)` is a pure function (`CallLLM | ExecuteTool
  | Finish`), same `assert_never` exhaustiveness pattern as `state.py`.
- Approval requests/parks correctly (`ApprovalRequested` instead of
  `ToolCallRequested`, matching spec section 13's exact flow) — verified
  it calls the LLM exactly once and doesn't loop. Resuming after
  `ApprovalGranted` is correctly left for Week 4.
- Two gaps called out directly in the code: `recovered` is hardcoded
  `False` (no real crash exercised yet), and no idempotency dedup check
  before executing a tool (Week 3 scope, matches the signature gap
  flagged in `refund_tools.py`).
- **Verified end-to-end**, not just type-checked: full scripted run
  through the real tools reached `RunCompleted` with the exact event
  trace shape from spec's worked example. This meets Week 2's "done
  when" bar. Full detail: `docs/BUILD_LOG.md` iteration 6.
- Full regression: `mypy --strict` clean across all 27 files, all 11
  existing tests still passing.

## Week 2, Day 1 (continued) — formal orchestrator tests, real bug caught and fixed

- `tests/unit/test_orchestrator.py` (5 tests, `InMemoryEventStore` fake —
  no real Postgres needed, orchestrator logic only): full run to
  completion, approval parks without looping, hallucinated tool recovers,
  step cap fails, cost cap fails.
- **Real bug found and fixed:** `state.py`'s `ToolCallFailed` handling
  left `state` unchanged (copied from `LLMCallFailed`'s reasoning, which
  doesn't apply here) — caused an infinite loop when a model hallucinates
  a nonexistent tool, since there's no `in_flight` to retry through and
  `decide_next_action` kept re-requesting the same bad tool forever.
  Fixed: `ToolCallFailed` now clears `in_flight` and feeds the error back
  as a `tool`-role message, same shape as `ToolCallCompleted`, so the
  model gets to react instead of the loop stalling. Caught by writing
  the orchestrator test, not by `state.py`'s own unit tests — a real
  integration-vs-unit-test lesson. Full detail: `docs/BUILD_LOG.md`
  iteration 7.
- All 16 tests passing, `mypy --strict` clean across 28 files.

## Week 2, Day 1 (continued) — replay CLI, Week 2 fully complete

- `cli.py` written: `replay <run_id>` subcommand (`argparse`, stdlib —
  chose over `typer` to avoid a new dependency for one small command).
  Prints every event as one readable line + a final summary (status,
  steps, tokens, cost, duration), sourced entirely from the event log.
- `.env.example` got its first real content: `DATABASE_URL=...`.
- Verified as a genuine subprocess invocation
  (`python -m durable_agents.cli replay <run_id>`) against a fresh real
  run — output matches spec's own worked-example table shape closely.
  Empty-run case handled gracefully.
- **This is Week 2's actual "done when" bar, met.** Full detail:
  `docs/BUILD_LOG.md` iteration 8.
- `mypy --strict` clean across 28 files, all 16 tests still passing.

**Week 2 is now fully complete.**

## Week 3, Day 1 — real idempotency dedup + injectable ledger backend

- `tools/registry.py`: `_build_parameters_schema` excludes a parameter
  named `idempotency_key` from the LLM-facing schema; `Tool` gained
  `needs_idempotency_key: bool`, auto-detected from the function
  signature. `orchestrator.py`'s `_reconcile()` now injects the real key
  when needed and reads `dedup_hit` back to set
  `ToolCallCompleted.provider_dedup_hit` correctly (previously hardcoded
  `False`).
- `tools/refund_tools.py` rewritten: `RefundBackend` Protocol,
  `InMemoryRefundBackend` (the only backend this project has — tracks
  `.attempts`/`.refunds` ledgers), `build_refund_tools(backend)` factory
  replacing the old hardcoded module-level tools.
- `tests/unit/test_refund_tools.py` (4 new tests) — the headline one
  proves the project's central assertion directly:
  `len(backend.attempts) == 2` but `len(backend.refunds) == 1` after
  issuing the same idempotency key twice.
- Full regression: `mypy --strict` clean across 29 files, 20/20 tests
  passing. Full detail: `docs/BUILD_LOG.md` iteration 9.

## Week 3, Day 1 (continued) — the chaos test suite: real SIGKILL, proven exactly-once

- Verified Windows has no `SIGKILL` before relying on a fallback: `os.kill()`
  there calls `TerminateProcess` for anything but Ctrl+C/Break — confirmed
  genuinely abrupt (code after the kill line never runs), just not named
  `SIGKILL`.
- `orchestrator.py`: two kill hooks (`kill_after_seq`,
  `kill_after_tool_execution_seq`) — the first tests "resume calls a
  never-yet-called tool exactly once," the second targets spec's actual
  "nastiest bug" (side effect already ran, nothing recorded it yet) since
  no event seq identifies that gap on its own.
- `tests/chaos/scenario_runner.py`: standalone subprocess entry point.
  Two real bugs caught while building it: `ScriptedLLM` needs to be
  sliced to the count of existing `LLMCallCompleted` events on resume
  (a fresh instance otherwise replays from the start); `kill_after_seq=0`
  originally had no code path that could fire, since `RunStarted` is
  appended before the `Orchestrator` (and its kill hook) exists.
- `tests/chaos/test_chaos.py`: 16 tests (15 kill points + the dedicated
  nastiest-bug case), all real subprocesses and real Postgres. All pass.
  The dedup mechanism was proven engaging via `dedup_hit: True` in the
  actual resumed trace, not just via a matching count.
- Full regression: `mypy --strict` clean across 32 files, 36/36 tests
  passing (~33s total). Full detail: `docs/BUILD_LOG.md` iteration 10.

## Week 3, Day 1 (continued) — start/resume as real CLI commands

- Extracted the canonical demo scenario (scripted responses +
  `RunStarted`) out of `scenario_runner.py` into
  `tools/refund_demo_scenario.py`, avoiding a second copy — both the
  chaos runner and `cli.py` now import the same source.
- `cli.py`: `start` and `resume` subcommands, both calling the same
  `_start_or_resume()` — mirrors `orchestrator.run()`'s own "starting is
  just resuming from empty" philosophy directly in the CLI.
- Verified `resume` genuinely picks up a manually-simulated interrupted
  run (only `RunStarted` present) — the resulting trace shows a real
  ~8-second gap between `seq 0` and `seq 1`, matching actual wall-clock
  time between the two separate commands.
- Honest limitation stated in the code: only runs the fixed demo
  scenario for now, since no real LLM client exists yet.
- Full regression: `mypy --strict` clean across 33 files, 36/36 tests
  passing. Full detail: `docs/BUILD_LOG.md` iteration 11.

## Week 3, Day 1 (continued) — real recovered detection, Week 3 fully complete

- `orchestrator.py`: `self._requested_this_run: set[int]`, reset per
  `run()` call. `recovered = state.in_flight.seq not in
  self._requested_this_run` — true whenever a dangling op was already
  sitting in the log before this invocation started, not appended by it.
- Verified against both a clean run (all `recovered=False`) and a real
  kill-and-resume (`CHAOS_KILL_AFTER_SEQ=11`) — the resumed process's
  completion correctly showed `recovered=True`, matching spec's own
  worked example exactly.
- Strengthened the chaos suite's nastiest-bug test with
  `assert recovered is True`.
- Separate finding, not fixed here: step/cost caps are checked *before*
  the in-flight check in the main loop — a run could hit its cap while a
  tool call is genuinely dangling, leaving that side effect permanently
  unresolved. Flagged for a future decision, not bundled into this change.
- Full regression: `mypy --strict` clean across 33 files, 36/36 tests
  passing (16/16 chaos). Full detail: `docs/BUILD_LOG.md` iteration 12.

**Week 3 is now fully complete.**

## Next — Week 4: Human-in-the-loop

Two of spec's five Week 4 items are already done (from Week 2):
`requires_approval` on tools, and clean park-and-exit on
`ApprovalRequested`. Three genuinely new pieces, in the order planned:

1. **Resuming after approval is granted** — `ApprovalGranted` currently
   just clears `pending_approval`/sets status back to `running`; nothing
   yet connects "this specific tool call was approved" to "now actually
   issue its `ToolCallRequested`." Needs real design — how does the
   orchestrator know a request that needed approval no longer does, for
   this one instance?
2. **The denial path** — `ApprovalDenied` also currently adds no message
   anywhere, so the model never learns it was rejected. Same fix shape as
   the `ToolCallFailed` bug from Week 2: feed the denial + reason back as
   a message so the model can react instead of the loop just stalling.
   (1) and (2) are tightly coupled — plan is to tackle them together.
3. **FastAPI: approve, deny, status endpoints** — there is currently *no
   way at all* to grant/deny an approval except manually appending the
   event in a script. This is what makes the demo real for someone other
   than us.
4. **Two-worker concurrency test** — proves the `(run_id, seq)` PK
   concurrency control works under real contention, not just Week 1's
   isolated test. Found while planning: `orchestrator.py` doesn't
   actually handle `ConcurrencyConflict` today — if two workers race,
   the loser's `_append()` would raise and crash `run()` outright rather
   than backing off and re-reading. This needs fixing as part of the
   test, not just tested around.

Planned order: (1)+(2) together, then FastAPI, then the concurrency fix
+ test.

## Week 4, Day 1 — approval grant/denial now actually resume the run

- Bug found before fixing: `ApprovalGranted`/`ApprovalDenied` cleared
  `pending_approval` but touched nothing else, so `decide_next_action`
  kept re-deciding the same tool call, which kept re-triggering
  `requires_approval()` — grant and denial were both silent no-ops.
- Denial fix: `state.py`'s `ApprovalDenied` case appends a `tool`-role
  message with the reason (same shape as the `ToolCallFailed` fix) —
  breaks the loop by changing `last.role`, no new field needed.
- Grant fix: new `RunState.approved_step: int | None`, set on
  `ApprovalGranted`, checked (and cleared) in the orchestrator's
  `requires_approval()` gate. Chosen over having the orchestrator rescan
  the event log — keeps the "state carries what's needed" discipline
  intact.
- Two new unit tests, both written and confirmed red before the fix:
  grant resumes the exact call exactly once (no double approval, no
  double execution); denial feeds the reason back and the tool never
  executes.
- Full regression: `mypy --strict` clean (29 files), 18/18 unit tests,
  16/16 chaos tests. Full detail: `docs/BUILD_LOG.md` iteration 13.

**Next: item 3 — FastAPI approve/deny/status endpoints.** Nothing calls
`store.append(ApprovalGranted/...)` yet except tests, by hand.

## Week 4, Day 2 — FastAPI approve/deny/status endpoints

- Flagged a real conflict first: `CLAUDE.md`'s "only the orchestrator
  appends events" didn't anticipate an HTTP endpoint recording a human
  decision. Resolved by narrowing the invariant's wording (in
  `CLAUDE.md` itself) rather than adding a pass-through orchestrator
  method just to satisfy the old wording literally.
- `src/durable_agents/api/app.py`: `create_app(store) -> FastAPI`.
  `GET /runs/{id}` (status), `POST /runs/{id}/approve`,
  `POST /runs/{id}/deny` — all append-only against the `EventStore`
  directly, no `Orchestrator` involved. 409 if the run isn't
  `awaiting_approval`, or on a `ConcurrencyConflict` race.
- New dependencies (asked first): `fastapi` (runtime), `httpx`
  (dev-only, for FastAPI's `TestClient`).
- 5 new tests in `tests/unit/test_api.py` — HTTP layer only; the
  approve/deny state-transition logic itself was already proven in
  Iteration 13's orchestrator tests.
- Full regression: `mypy --strict` clean (30 files), 23/23 unit tests,
  16/16 chaos untouched. Full detail: `docs/BUILD_LOG.md` iteration 14.

**Next: item 4 — two-worker concurrency test.** Needs `orchestrator.py`
fixed first: its main loop doesn't currently handle `ConcurrencyConflict`
at all — a racing worker's `_append()` would raise and crash `run()`
rather than back off and re-read.

## Week 4, Day 2 (continued) — ConcurrencyConflict handling + two-worker test

- `orchestrator.py`: `_append()` now catches `ConcurrencyConflict` and
  returns; no other code changed, since `run()`'s main loop already
  re-reads events fresh at the top of every iteration regardless of
  what happened in the previous one.
- `tests/integration/test_concurrent_workers.py`: two `Orchestrator`s,
  each with its own real `asyncpg` pool against the same Postgres
  container, raced via `asyncio.gather` on one run. Deliberately not
  built on the in-memory fake (its calls do no real I/O, so two
  gathered coroutines never actually interleave — no genuine race).
  Confirms: both workers converge to `completed` with the same answer,
  and a `GROUP BY type` count on the real `events` table shows exactly
  one row of each event type despite the race. Ran 5x to rule out
  flakiness — stable every time.
- Full regression: `mypy --strict` clean (35 files), 44/44 tests
  (23 unit, 5 integration, 16 chaos). Full detail: `docs/BUILD_LOG.md`
  iteration 15.

**Week 4 is now fully complete.**

## Next — Week 5: Guardrails

Per spec section 18:
- Threat model written down first, before any code
- L1 input scan: patterns, PII detection and redaction
- L2 tool-result scan and delimiting — spec calls this "the layer that
  matters"
- L3 output validation: schema, allowlist, policy bounds
- L4 run-level: loop detection, escalation
- `GuardrailTriggered` events throughout (event + `state.py` handling
  already exist, from Week 1/2 — this week is the actual detection logic)
- Attack corpus (50-100 labelled) + benign corpus
- Measure attack success rate **and** false positive rate

`guardrails/decisions.py` is the user's last untouched write-yourself
file.

**Threat model written (2026-09-02):** `docs/THREAT_MODEL.md` — the six
threat categories mapped to this project's actual surface (goal text,
the three refund tools), which layer catches what, this week's
deliberate scope cuts, and a first proposal for `strict`/`standard`/
`lenient` guardrail_profile thresholds (not binding — `decisions.py`'s
call). Full detail: `docs/BUILD_LOG.md` iteration 17.

**Correction, same day:** first draft hardcoded India-specific PII
patterns (PAN/Aadhaar/IFSC) as if they belonged in the core engine —
copied from spec's example without questioning it. User caught this as
bias against a library meant to ship worldwide. Fixed: PII patterns are
now a pluggable parameter, core ships only locale-neutral defaults
(credit card, email, phone, IBAN); country-specific patterns are
consumer config. Same fix applied to the policy cap (config, not a
hardcoded ₹ constant). Doc updated in place.

## Week 5, Day 1 (continued) — L1 input scan built

- `guardrails/types.py`: `GuardMatch`, `ScanResult` — detection-only,
  no `GuardAction` (that's `decisions.py`).
- `guardrails/patterns.py`: shared injection-pattern list + PII pattern
  list (locale-neutral: email, Luhn-validated credit card, IBAN, phone)
  + `scan_pii()`'s redact-and-restore logic. Built shared, not
  duplicated, since L2 needs the identical logic later this week.
- `guardrails/input_scan.py`: `scan_input(goal, pii_patterns=...)` —
  plain function, not a class (spec's sketch shows a class; diverged
  deliberately, see `docs/BUILD_LOG.md` iteration 18 for reasoning).
- 10 new tests, including a Luhn-invalid number correctly *not* being
  flagged as a credit card.
- Full regression: `mypy --strict` clean (33 files), 55/55 tests.
- Not yet wired into `orchestrator.py` — needs `decisions.py` to exist
  first to decide what action a match produces. Full detail:
  `docs/BUILD_LOG.md` iteration 18.

## Week 5, Day 1 (continued) — L2 tool-result scan built

- `guardrails/tool_result_scan.py`: `scan_tool_result(tool, result)` —
  thin wrapper over the same shared `patterns.py`, proving the L1/L2
  shared-module bet paid off (no new detection logic needed). Plus
  `wrap_untrusted(tool, content)` — the delimiter template, applied
  unconditionally to every tool result regardless of scan outcome
  (structural hygiene, not a `GuardAction`).
- 5 new tests: catches the indirect-injection example from the earlier
  walkthrough, redacts PII in tool output, delimiter wraps even clean
  content.
- Full regression: `mypy --strict` clean (34 files), 39/39 unit tests.
- Still not wired into the orchestrator — same reason as L1, needs
  `decisions.py`. L1+L2 detection is now fully done; L3+L4+`decisions.py`
  + orchestrator wiring are next, together.

## Week 5, Day 1 (continued) — L3 + L4 detection built

- `registry.py`: `Tool` gained `args_model` (the Pydantic model it was
  already building internally, now kept instead of discarded) — lets
  L3 validate a live call's arguments without a new `jsonschema`
  dependency.
- `guardrails/output_validate.py`: `validate_output(tool_call, tools,
  policy_caps=None)` — allowlist, schema, policy bounds (config, not a
  hardcoded ₹ constant), PII-in-model-output. All four of spec's L3
  checks.
- `guardrails/run_level.py`: `detect_loop` (same tool+args N times →
  flag) and `detect_escalation` (N guardrail hits this run → force
  approval regardless). Both pure over `RunState`.
- 11 new tests. Full regression: `mypy --strict` clean (36 files),
  71/71 tests (unit + integration + chaos all green).
- **All four guardrail layers are now built and tested standalone.**
  Nothing wired into `orchestrator.py` yet — that's the last piece:
  `decisions.py` (your file) + the actual hook points + `GuardrailTriggered`
  events. Full detail: `docs/BUILD_LOG.md` iteration 20.

## Week 5, Day 1 (continued) — decisions.py + orchestrator wiring: guardrails are live

- `guardrails/decisions.py` (write-yourself file, user explicitly asked
  me to write it this time): `GuardrailProfile`, three named profiles
  (strict/standard/lenient), `decide(match, profile) -> GuardrailAction`.
  Deterministic violations always BLOCK; only confidence-based injection
  matches vary by profile. 12 tests.
- Orchestrator wired at 4 hook points: L1 (once, gated on `state.step
  == 0`), L3+L4-loop (in `_request_tool_call`, before the approval
  gate), L4-escalation (OR'd into the existing approval gate — reuses
  Week 4's grant/deny machinery), L2 (in `_reconcile`, after tool
  execution). New `_sanitize_for_llm`: redaction/delimiting applied
  fresh at every LLM call, never by mutating stored history.
- **Known cut:** L1 `ESCALATE` has no tool-call context to attach a
  human approval request to yet — fails closed (`BLOCK`) instead of
  parking, until that's given a real design pass.
- **Real bug found and fixed:** a random `uuid4()`-based `refund_id`
  occasionally welded with `amount_inr` into a string that passed the
  Luhn check by chance, causing genuinely intermittent chaos-suite
  failures (16/16 sometimes, 13/16 other times). Root cause: naive
  `" ".join(...)` on tool-result fields. Fixed by scanning
  `json.dumps(...)` instead — quotes/commas break the false adjacency.
  2 regression tests prove both the bug and the fix.
- 2 new orchestrator tests: a ₹5,00,000-over-cap refund gets blocked
  before executing (the design discussion's own worked example, now a
  real test), and 3 unrelated PII hits force approval on a refund
  *below* the tool's own approval threshold.
- Full regression: `mypy --strict` clean (37 files), 86/86 tests, chaos
  suite re-run 4x clean after the fix. Full detail: `docs/BUILD_LOG.md`
  iteration 21.

**Guardrails are now fully live.** Remaining for Week 5: the attack
corpus (50-100 labelled cases) + benign corpus, and the actual
attack-success-rate / false-positive-rate measurement.

## Week 5, Day 1 (continued) — runnable attack demo

- `examples/demo_guardrail_block.py` — real Postgres store + refund
  backend, a scripted ₹5,00,000 refund request against the ₹6,400 demo
  order. `durable-agents replay <run_id>` shows the real
  `GuardrailTriggered`/`RunFailed(guardrail_block)` trace.
- New top-level `examples/` directory — same "don't ship demo code in
  the wheel" reasoning already flagged for `refund_tools.py`. Also
  reusable for Week 6's "Inject attack" demo-page button.
  Full detail: `docs/BUILD_LOG.md` iteration 22.

## Week 5, Day 1 (continued) — attack corpus + real eval numbers, Week 5 fully complete

- `tests/guardrails/corpus.py`: 80 labelled cases (10 per threat
  category × 6 + 20 benign, several benign ones deliberately
  suspicious-looking). Found `tests/guardrails/`/`live/`/`property/`
  already existed as empty Week-1 scaffolding — this is where it
  belonged.
- `tests/guardrails/test_corpus_eval.py`: measures the detection+
  decision layer directly (design choice confirmed with user first).
  Real numbers:

  | Profile | Attack success rate | False positive rate |
  |---|---|---|
  | strict | 0% | 25% |
  | standard (financial_v1) | 0% | 20% |
  | lenient | 25% | 5% |

- First run: 52/60 caught (13% success). 8 misses were pure detection
  gaps (phrasings matching nothing at all) — added 6 generalized
  patterns, re-ran: 60/60 under standard/strict.
- 20% false-positive rate under standard was investigated, not tuned
  away — `bn-07` gets BLOCKed because "disregard the previous..." is
  genuinely ambiguous (common innocent correction AND common attack
  opener). Documented honestly rather than weakened, since weakening it
  would let a real attack through too.
- Full regression: `mypy --strict` clean (44 files), 88/88 tests, chaos
  re-verified 2x clean. Full detail: `docs/BUILD_LOG.md` iteration 23.

**Week 5 is now fully complete.** `guardrails/decisions.py` — the
user's last write-yourself file — is written, by explicit request.

## Week 6, Day 1 — error handling, retries, system prompt, logging

Prompted by a full-project review. The finding no doc had recorded:
**`LLMCallFailed` was never appended by anything**, and there was
exactly one `try/except` in `orchestrator.py`. A 429 or a tool timeout
crashed the process outright.

- **Retry budget in the event log**, not a local variable —
  `InFlightOp.attempts`, incremented by `LLMCallFailed`/`ToolCallFailed`.
  A resumed process inherits what a dead one spent; a local counter
  would reset on every crash and retry forever.
- **Tool retries reuse the original idempotency key.** This closed a
  real exactly-once hole: surfacing a tool error to the model made it
  issue a fresh call at a new seq — a *new* key nothing would
  deduplicate, i.e. a double charge. `ToolCallFailed.final_attempt`
  (default `True`, so old rows rebuild identically) distinguishes
  "retrying in place" from "budget spent, tell the model."
- **System prompt now exists.** `RunStarted.system_prompt` stored in
  full; `system_prompt_hash` derived from it rather than hand-supplied
  so they can't disagree (and old rows keep their stored hash).
  Threaded through `RunState` into `LLMClient.call()`. Previously the
  log had been fingerprinting a prompt that didn't exist for 5 weeks,
  and the agent couldn't be steered at all.
- **Logging** via `logging.getLogger(__name__)`, no handler attached.
- **Two bugs the logging immediately exposed:** `recovered` was
  meaninglessly always-`True` for LLM ops (`_requested_this_run` only
  tracked tool seqs); and `replay` crashed with `UnicodeEncodeError` on
  any non-ASCII output on Windows (cp1252) — i.e. on most of the
  world's text.
- 8 new tests + `examples/demo_retry_recovery.py` (real Postgres, two
  provider errors and a tool timeout, run still completes, exactly one
  charge). Full regression: `mypy --strict` clean (45 files), 96/96
  tests, chaos verified stable 2x. Full detail: `docs/BUILD_LOG.md`
  iteration 24.

**Still open from that review**, in priority order: empty
`__init__.py` (no public API — `from durable_agents import ...` gives
nothing), empty `README.md` and `DECISIONS.md`, schema not shipped in
the wheel (`db/migrations/*.sql` is outside the packaged directory, so
`pip install` yields something that can't create its own table), no
in-memory `EventStore` in the package (can't try it without Docker),
no `AnthropicClient`, no `POST /runs`, no recovery sweeper.

## Week 6: Ship it

Reordered after the 2026-09-02 project review. Spec's own Week 6 list
is kept, but **the review findings are sequenced ahead of the
demo/marketing items**, because the stated goal is real usage rather
than a published-looking repo — and several findings mean the package
is currently not installable-and-usable at all.

The review's core conclusion: *the durable-execution engine is solid
and unusually well-tested; the library around it does not exist yet.*
That gap is the week's actual work.

---

### Phase 0 — production-correctness hardening ✅ DONE (Iteration 24)

Retries + error events, tool retry reusing the idempotency key, system
prompt, logging. Plus two bugs the logging exposed (`recovered` always
true for LLM ops; `replay` crashing on non-ASCII on Windows). See the
Week 6 Day 1 entry above.

---

### Phase 1 — make it an actual library ✅ DONE (Iteration 25)

All six items closed, plus two packaging bugs found along the way.

- **`Runtime` facade built** (`runtime.py`): `create()` records,
  `start()` records+executes, `resume()`, `get_state()`. Tools supplied
  once at construction rather than per-`start()` as spec shows —
  nothing in the log records which tools were registered, so per-run
  tools would let a resume run against a different set than produced
  the events.
- **Public API**: 47 exports with explicit `__all__`; a test asserts
  every advertised name resolves.
- **Schema ships**: `storage/schema.sql` inside the package,
  `create_schema(dsn)` + `schema_sql()` + `durable-agents init-db`.
  Verified by building the wheel and listing its contents.
- **`InMemoryEventStore` ships** (`storage/memory.py`), mirroring
  Postgres's `ConcurrencyConflict` semantics.
- **README written** — including an Honest limits section and a
  comparison table that says when to use Temporal instead. Its example
  is executed by the test suite so it can't rot.
- **`DECISIONS.md` populated** from the real forks across 24
  iterations, with rejected alternatives and open questions.
- **Two packaging bugs fixed**: there was no console-script entry point
  at all (every doc saying `durable-agents replay …` was wrong), and
  `fastapi` was a hard runtime dep despite the runtime never importing
  it (now an optional `api` extra). Added license/keywords/classifiers.
- 9 new tests importing only from the top-level package. Full
  regression: `mypy --strict` clean (49 files), 105/105 tests. README
  quickstart run verbatim through the installed console script.
  Full detail: `docs/BUILD_LOG.md` iteration 25.

<details>
<summary>Original Phase 1 item list (all closed)</summary>

Right now `pip install durable-agents` produces something a stranger
cannot use. Every item here is a hard blocker:

1. **Public API in `src/durable_agents/__init__.py`** — currently
   empty, so `from durable_agents import ...` yields nothing and every
   consumer must import internal module paths
   (`durable_agents.storage.postgres`, `durable_agents.tools.registry`,
   …). Decide and export the real surface: `tool`, `Orchestrator` (or a
   friendlier `Runtime` facade), `EventStore`/`PostgresEventStore`,
   `LLMClient`/`LLMResponse`, `RunState`, the guardrail profiles.
   Set `__all__`.
2. **Ship the schema.** `db/migrations/*.sql` lives outside
   `src/durable_agents`, which is the only packaged directory — so the
   wheel contains no way to create the `events` table, and there is no
   migration runner anywhere. Move the SQL inside the package as data
   files and expose `create_schema(dsn)` plus a
   `durable-agents init-db` CLI command.
3. **Ship an in-memory `EventStore`.** It exists only inside two test
   files today, so nobody can try the library without standing up
   Docker + Postgres first. Most evaluators quit at that step.
4. **Write `README.md`** — currently 0 bytes, so the PyPI page would be
   blank. Needs: the problem in three sentences, a genuinely
   copy-pasteable working example, install + `init-db`, honest limits,
   and the comparison table below.
5. **Reconcile the "five-line example" with reality.** Spec section 17
   advertises `Runtime`, `PostgresStore`, `GuardrailProfile.financial()`
   — none of which exist under those names or shapes. Either build that
   facade or rewrite the promise. Do not publish a README whose
   headline example doesn't run.
6. **`DECISIONS.md`** is also 0 bytes despite `BUILD_LOG.md`
   advertising it as the "why this over the alternative" companion.
   Either populate it from the decisions already recorded across
   iterations, or drop the reference.

</details>

---

### Phase 2 — make it run a real agent (NEXT)

7. **`AnthropicClient`** (deferred twice — silently in Week 2, then
   explicitly in Week 5). Until this exists the runtime can only replay
   one fixed scripted scenario; it cannot be pointed at a real model.
   Note the burden this puts on third-party implementers:
   `LLMResponse` demands `cost_usd: Decimal` and `provider_request_id`,
   which a custom provider may have to fabricate — worth revisiting
   while writing the first real implementation.
8. **Live-tests tier** — `@pytest.mark.live`, `skipif` without an API
   key so a fresh clone still runs green, hard cost cap, tools always
   fake, never in CI on every push.
9. **`POST /runs`** — the API can read status and approve/deny, but
   cannot *start* a run. Today that only works via CLI or direct Python
   calls, which makes the HTTP surface incomplete for any real
   deployment.
10. **L1 classifier sub-layer** (unblocked once (7) lands) — the
    "is this text trying to manipulate an AI system?" check that
    pattern matching structurally cannot do. Optional, off by default,
    cached by input hash.

---

### Phase 3 — make it self-healing (the actual production story)

11. **Recovery sweeper** — a periodic job running the dangling-
    `Requested` query and re-enqueueing orphaned runs. This is what
    turns crash-resume from a manual CLI command into an automatic
    property of the system, and it is the single most quotable thing in
    the whole project ("a pod dies, a minute later something else picks
    the run up"). Not built at all.
12. **Dockerfile + deploy** — API + worker + sweeper as three
    processes, per spec's own architecture.

---

### Phase 4 — make it findable and understandable

13. **Demo page** with kill and inject-attack buttons, SSE streaming.
    `examples/demo_guardrail_block.py` and
    `examples/demo_retry_recovery.py` are head starts on two of the
    buttons.
14. **`replay` polish** — cost/duration/guardrail rollup summary.
    `_event_detail` already formats every event type, so this is
    aggregation, not new formatting.
15. **Comparison table** — vs Temporal, Restate, DBOS, LangGraph
    checkpointers. Scrupulously fair, including where this is worse
    (single language, no distributed workers, Postgres-only). Needs a
    real answer to "why not Temporal", not just "simpler".
16. **GIF, two-minute video, blog post, PyPI publish.** Publish *after*
    Phase 1, not before — an empty-README package with no public API is
    worse than no package.

---

### Packaging cleanup (before any publish)

17. Move `tools/refund_tools.py`, `tools/refund_demo_scenario.py`, and
    `tools/refund_backend_postgres.py` out of `src/durable_agents/` —
    demo application code that shouldn't ship in the wheel. `examples/`
    now exists as a natural destination. `tools/registry.py` (the
    generic decorator) stays.
18. Decide whether `002_refund_ledger.sql` ships at all — it's demo
    schema, not runtime schema.

---

### Open design questions the review raised (decide, don't drift)

- **Incremental adoption.** The current API demands a consumer hand
  over their entire agent loop: implement `LLMClient`, re-declare every
  tool through `@tool`, restructure control flow around
  `Orchestrator.run()`, run Postgres. Everyone who wants this already
  has a working agent loop, and all-or-nothing migration is the main
  reason agent frameworks get starred and never used. A wrapper API
  (`async with runtime.run(...) as run: await run.llm_call(...)` /
  `await run.tool_call(...)`) would allow adopting durability
  incrementally against an existing loop. Potentially the single
  highest-leverage change for real usage; needs a real decision.
- **Guardrail positioning.** Regex-based prompt-injection detection is
  not credible as a headline feature — the eval's own honest 20%
  false-positive rate shows why. Consider demoting guardrails to an
  optional module so they don't undercut the durability work, which is
  the genuinely differentiated part.
- **Spec bias.** `docs/SPEC.md` optimizes for *appearing* shipped
  ("nobody checks download counts; installability is the signal";
  "worth being able to say in an interview") and defers every adoption
  task to this final week. That trade-off is worth making explicitly
  now that the stated goal is real users.
- **Known correctness gap, still open:** step/cost caps are checked
  *before* the in-flight check in `run()`'s loop, so a run can hit its
  cap while a tool call is genuinely dangling, leaving that side effect
  unresolved (flagged back in Iteration 12).
- **L1 `ESCALATE` fails closed** (BLOCK) because there's no tool call
  in context to attach an `ApprovalRequested` to. Needs either a schema
  change or a deliberate decision to keep failing closed.

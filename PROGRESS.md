# Progress

## Week 1, Day 1 — Project scaffolding

- Read `docs/SPEC.md`, confirmed understanding with user.
- Created repo skeleton: `src/durable_agents/` package layout (events, state,
  orchestrator, storage, llm, tools, guardrails, api, cli), `db/migrations/`,
  `tests/` (unit, integration, chaos, guardrails, property, live), plus
  `pyproject.toml`, `docker-compose.yml`, `DECISIONS.md`, `README.md`,
  `.env.example`, `.gitignore`. All files empty — no application code yet.
- Created `CLAUDE.md`: source-of-truth pointer, core invariants, do-not-
  implement list (events.py, state.py, orchestrator.py,
  guardrails/decisions.py — user writes these), stack, dependency rule.
- `uv.lock` intentionally not created — generated by `uv` once
  `pyproject.toml` has real dependencies.

## Week 1, Day 1 (continued) — Postgres, dependencies, events.py

- `docker-compose.yml`: Postgres 16 (running, healthy) + Adminer web UI at
  `localhost:8080` for browsing the DB visually.
- `db/migrations/001_events_table.sql` applied to the running container.
  Dropped one index from the literal spec text (`idx_events_run_seq`) after
  confirming it duplicated the index the primary key already creates —
  first instance of "verify the spec, don't transcribe it."
- Installed `uv` (wasn't on the machine), wrote `pyproject.toml`
  (`hatchling` build backend, `pydantic`/`asyncpg` runtime deps,
  `pytest`/`pytest-asyncio`/`testcontainers[postgres]`/`mypy` dev deps),
  ran `uv sync` → `.venv` created, `uv.lock` generated (committed;
  `.venv` is not — filled in `.gitignore`, which had been left empty).
- `src/durable_agents/events.py` written: `BaseEvent`, `ToolCallInvocation`
  sub-model, all 13 event classes, `Event` discriminated union. Four design
  decisions made via structured Q&A (nested models over dicts, closed
  `Literal` types over open strings, flexible dict for
  `GuardrailTriggered.detail`, dropped a redundant timestamp field). Filled
  a spec gap: `ToolCallFailed` needed more fields than the spec table
  listed to be reconcilable on resume. Verified with `mypy --strict`
  (clean) and a manual discriminated-union round-trip test.
  Note: this file was on the user's own "write yourself" list; written by
  Claude only after the user was shown that tradeoff explicitly and chose
  to override it anyway. Full detail in `docs/BUILD_LOG.md` iteration 2.

## Week 1, Day 3 — EventStore built and verified live

- `storage/protocol.py`: `EventStore` as an ABC (user chose this over the
  spec's `Protocol`), `ConcurrencyConflict` exception.
- `storage/postgres.py`: real implementation. Pool injected via
  constructor + `connect(dsn)` convenience classmethod; payload JSONB
  strips `seq`/`created_at`/`type` (already columns) and merges them back
  on read. Added `asyncpg-stubs` dev dependency after `mypy --strict`
  flagged asyncpg has no type stubs at all.
- Verified live against the running Postgres container (append, read,
  round-trip equality, `ConcurrencyConflict` on duplicate seq, empty
  `read_since`) — all passed. Manual script, not yet a committed test.
- Hit and fixed an environment issue: a native Windows PostgreSQL 18
  service was squatting on port 5432, so the container's port-forward
  never actually received host connections. Stopped the native service.
  Full detail in `docs/BUILD_LOG.md` iteration 3 — worth remembering if
  Postgres connection errors show up again on this machine.

## Week 1, Day 3 (continued) — state.py, all tests green, Week 1 done

- `state.py` written (`RunState`, `Message`, `InFlightOp`,
  `PendingApproval`, `GuardrailHit`, `apply()`, `rebuild_state()`) — same
  override pattern as `events.py`: flagged the conflict with `CLAUDE.md`,
  user chose full override. One structural spec fix: dropped `run_id`
  from `RunState` since `rebuild_state` never receives one and `Event`
  objects don't carry it either — documented in the docstring.
- `tests/unit/test_state.py` (7 tests) and
  `tests/integration/test_postgres_store.py` + `conftest.py` (4 tests,
  real disposable Postgres via `testcontainers`) — all 11 passing,
  `mypy --strict` clean across all 27 source files.
- Two gotchas hit and fixed: asyncpg pools can't cross event loops (broke
  a session-scoped pool fixture under pytest-asyncio strict mode — fixed
  by making the pool function-scoped, only the container stays
  session-scoped), and mypy needs a `py.typed` marker file to trust a
  locally-installed package's own type hints once it's imported rather
  than analyzed by path (added `src/durable_agents/py.typed`).
- **Week 1 is functionally complete** per spec section 18's scope. Full
  detail: `docs/BUILD_LOG.md` iteration 4.

## Week 2, Day 1 — LLM client + tool registry built

- `llm/protocol.py`: `LLMClient` ABC (consistent with `EventStore`'s
  ABC choice — asked again rather than assumed), `LLMResponse` reusing
  `ToolCallInvocation` from `events.py`.
- `llm/scripted.py`: `ScriptedLLM`. Ships in the real package (unlike
  `refund_tools.py`) — generic testing infra any consumer needs, not
  demo-specific.
- `tools/registry.py`: `@tool` decorator, JSON schema auto-derived from
  function type hints via Pydantic's `create_model`. Dropped the spec's
  `idempotency=` parameter — its own formula never actually used the
  value. `idempotency_key()` implements spec section 11's sha256 formula.
- `tools/refund_tools.py`: `lookup_order`, `check_refund_policy`,
  `issue_refund` — in-memory demo data, no attempt-ledger yet (that's
  Week 3, once crash-resume testing needs it).
- All verified manually (schema derivation, approval threshold,
  idempotency key stability/uniqueness, tool execution). `mypy --strict`
  clean. Full detail: `docs/BUILD_LOG.md` iteration 5.

## Week 2, Day 1 (continued) — orchestrator.py built and verified end-to-end

- `orchestrator.py` written (fourth and last write-yourself file, same
  override pattern as the previous three). Key insight: reconciliation
  (spec's step 2) and normal operation are literally the same code path
  — the loop reloads state fresh every iteration and can't structurally
  tell "requested a moment ago" from "requested by a dead process."
  `decide_next_action(state)` is a pure function (`CallLLM | ExecuteTool
  | Finish`), same `assert_never` exhaustiveness pattern as `state.py`.
- Approval requests/parks correctly (`ApprovalRequested` instead of
  `ToolCallRequested`, matching spec section 13's exact flow) — verified
  it calls the LLM exactly once and doesn't loop. Resuming after
  `ApprovalGranted` is correctly left for Week 4.
- Two gaps called out directly in the code: `recovered` is hardcoded
  `False` (no real crash exercised yet), and no idempotency dedup check
  before executing a tool (Week 3 scope, matches the signature gap
  flagged in `refund_tools.py`).
- **Verified end-to-end**, not just type-checked: full scripted run
  through the real tools reached `RunCompleted` with the exact event
  trace shape from spec's worked example. This meets Week 2's "done
  when" bar. Full detail: `docs/BUILD_LOG.md` iteration 6.
- Full regression: `mypy --strict` clean across all 27 files, all 11
  existing tests still passing.

## Next — Week 2 wrap-up / Week 3 prep

- Formal committed test file for the orchestrator (current verification
  is a manual script, not `tests/`)
- Polished `replay <run_id>` CLI command
- Week 3: in-flight reconciliation is already built — next is real
  idempotency dedup + fake tool APIs with attempt ledgers + chaos tests
